import { BrowserRouter, Route, Routes } from "react-router-dom";

export default function Autentificado() {
  return (
    <BrowserRouter>
      <Routes>
        <Route path="/"/>
      </Routes>
    </BrowserRouter>
  )
}