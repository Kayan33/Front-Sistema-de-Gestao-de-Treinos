import { BrowserRouter, Route, Routes } from "react-router-dom";

export default function Nautentificado() {

  return (
    <BrowserRouter>
      <Routes>
        <Route path="" />
      </Routes>
    </BrowserRouter>
  )
}